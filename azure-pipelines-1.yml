resources:
  pipelines:
    - pipeline: building_pipeline
      source: build_pipeline
      project: https://dev.azure.com/hakobmkoyan771
      trigger: true

pool:
  Default

jobs:
  - job: debug
    steps:
      - script: |
          echo "Hello World!"